server:
  port: 8080
  servlet:
    context-path: /
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/boss_train?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: admin
